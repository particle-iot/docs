$(document).ready(function () {
    // auth not required

    // Possibly move this to a JSON file
    const batteryInfo = {
        devices: [
            
        ]
    }

    $('.batteryCalculator').each(function() {
        const thisElem = $(this);
        console.log('batteryCalculator');
    });
});